{
  "t": [
    "    { data: u = [] } = mn({ queryKey: [\"/api/tickets\"] }),",
    "        pt.invalidateQueries({ queryKey: [\"/api/tickets\"] });"
  ],
  "s": [
    "    { data: Q = [] } = mn({ queryKey: [\"/api/settings\"] }),",
    "    { data: r = [] } = mn({ queryKey: [\"/api/settings\"] }),",
    "        (await ct(\"PUT\", `/api/settings/${I}`, { value: ie })).json(),",
    "        pt.invalidateQueries({ queryKey: [\"/api/settings\"] });"
  ]
}